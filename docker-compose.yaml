version: "3.9"

services:
  ai:
    build: .
    volumes:
      - ./app:/app/app
      - ./scripts:/app/scripts
      - ./notebook:/app/notebook
      - ./datas:/app/datas
      # Optional: mount Kaggle credentials for automatic download
      # - ~/.kaggle:/root/.kaggle
